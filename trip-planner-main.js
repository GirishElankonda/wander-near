/**
 * WanderNear Trip Planner - Vite entry point
 *
 * Firebase auth module so the trip planner page can
 * check auth state for the user menu.
 */

// Firebase auth (ESM - import.meta.env injected by Vite)
import './firebase-config.js';
import './auth.js';
